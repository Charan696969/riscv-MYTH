#include <stdio.h>
#include <math.h>

int main(){
        unsigned long long max = (unsigned long long) (pow(2,64)-1);
        printf("Longest 64-bit unsigned number(unsigned long long) is $
        return 0;
}
